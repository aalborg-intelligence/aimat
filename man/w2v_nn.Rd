% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/w2v_nn.R
\name{w2v_nn}
\alias{w2v_nn}
\title{Word2vec Neural Network}
\usage{
w2v_nn(X, Y, hidden_dim, epochs, learning_rate, weights = NULL, verbose = 10)
}
\arguments{
\item{X}{Matrix of one-hot encoded input vectors}

\item{Y}{Matrix of one-hot encoded output vectors}

\item{hidden_dim}{Dimension of the hidden layer}

\item{epochs}{Number of training epochs}

\item{learning_rate}{Learning rate}

\item{weights}{Initial weights for the network (random if NULL)}
}
\value{
Named list contaning trained weights for the network
}
\description{
Word2vec Neural Network
}
\examples{
# Example usage
library(Matrix)
n <- 10  # Dimension of input and output
m <- 3   # Dimension of the first hidden layer

# Create input matrix with several one-hot vectors
X <- Matrix(0, nrow = 5, ncol = n, sparse = TRUE)
for (i in 1:5) {
  X[i, sample(1:n, 1)] <- 1
}

# Create output matrix with corresponding one-hot vectors
Y <- Matrix(0, nrow = 5, ncol = n, sparse = TRUE)
for (i in 1:5) {
  Y[i, sample(1:n, 1)] <- 1
}

# Train the network
trained_weights <- w2v_nn(X, Y, m, epochs = 100, learning_rate = 0.1, verbose = 10)
}
